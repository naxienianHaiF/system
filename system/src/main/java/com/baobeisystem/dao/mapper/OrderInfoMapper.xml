<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.baobeisystem.dao.waiwang.DBAreaDao">

	<!--  -->
	<insert id="insertOrder" parameterType="OrderInfo">
	
	</insert>
	
	<!--  -->
	<select id="getByOusername" parameterType="string" resultType="OrderInfo">
		select * from  where ousername=#{ousername}
	</select>
</mapper>